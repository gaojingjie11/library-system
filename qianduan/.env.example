# Defaults to same-origin API. Development server proxies /api and /upload(s).
VUE_APP_API_BASE_URL=
API_PROXY_TARGET=http://127.0.0.1:3000
# Production: reverse proxy /api, /upload and /uploads to the backend.
